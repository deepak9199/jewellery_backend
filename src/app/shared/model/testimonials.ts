export interface testimonials {
    name: string
    comment: string
    createdTime: String
}
export interface testimonials {
    id: string
    name: string
    comment: string
    createdTime: String
}